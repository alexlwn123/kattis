import sys
import math
def main():
	lines = sys.stdin.readlines()
	
	for line in lines

		if len(line) == 1:
		instructs = 
		directions = int(line)
		for i in range(dircetions)

if __name__ == '__main__':
	main()